services:
  - type: web
    name: langflow
    env: python
    region: oregon
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python src/backend/base/langflow/main.py"
